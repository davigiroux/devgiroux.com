---
title: "MDX Configuration Test"
description: "Testing all MDX features including syntax highlighting, custom components, and embeds"
date: "2025-12-30"
tags: ["mdx", "testing", "configuration"]
published: true
---

# MDX Configuration Test

This is a comprehensive test of all MDX features we've implemented.

## Syntax Highlighting

Here's a TypeScript code block with syntax highlighting:

```typescript
interface User {
  id: string;
  name: string;
  email: string;
}

async function fetchUser(userId: string): Promise<User> {
  const response = await fetch(`/api/users/${userId}`);
  if (!response.ok) {
    throw new Error('Failed to fetch user');
  }
  return response.json();
}

const user = await fetchUser('123');
console.log(user.name);
```

## Custom Components

### Links

Here's an [internal link](/blog) and an [external link](https://github.com).

### Inline Code

You can use inline code like `const foo = 'bar'` within paragraphs.

### Blockquote

> This is a blockquote. It should be styled with a violet border and italic text.
> It can span multiple lines.

### Lists

Unordered list:
- First item
- Second item
- Third item

Ordered list:
1. First step
2. Second step
3. Third step

### Table

| Feature | Status | Notes |
|---------|--------|-------|
| Syntax Highlighting | ✅ | Using Shiki with github-dark theme |
| Custom Components | ✅ | Headings, links, code blocks |
| YouTube Embeds | ✅ | Responsive 16:9 ratio |

### Horizontal Rule

---

## Callout Examples

import { Callout } from '@/components/mdx/callout';

<Callout type="info">
This is an informational callout. Use it to provide helpful tips or additional context.
</Callout>

<Callout type="warning">
This is a warning callout. Use it to alert users about potential issues or important notes.
</Callout>

<Callout type="error">
This is an error callout. Use it to highlight critical information or errors that need attention.
</Callout>

## YouTube Embed

import { YouTubeEmbed } from '@/components/mdx/youtube-embed';

<YouTubeEmbed id="dQw4w9WgXcQ" title="Sample Video" />

## More Code Examples

JavaScript with JSX:

```jsx
function Welcome({ name }) {
  return (
    <div className="greeting">
      <h1>Hello, {name}!</h1>
      <p>Welcome to our site.</p>
    </div>
  );
}
```

Python:

```python
def calculate_fibonacci(n):
    if n <= 1:
        return n
    return calculate_fibonacci(n-1) + calculate_fibonacci(n-2)

result = calculate_fibonacci(10)
print(f"Fibonacci(10) = {result}")
```

## Conclusion

This test file demonstrates all the MDX features:
- ✅ Syntax highlighting with Shiki
- ✅ Custom headings with anchor links
- ✅ Styled links (internal and external)
- ✅ Code blocks with copy button
- ✅ Inline code styling
- ✅ Blockquotes
- ✅ Lists (ordered and unordered)
- ✅ Tables
- ✅ Callout components
- ✅ YouTube embeds
